Frontend README

Install:
  cd frontend
  npm install

Run dev server:
  npm run dev
Default port: 5173

The frontend fetches recommend endpoint at VITE_API_BASE (default http://localhost:8000).
You can set VITE_API_BASE in environment (or in docker-compose) to point to backend.
